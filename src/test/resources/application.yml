spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://no-route:5432/your_account_db
    username: no-user
    password: no-pass

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: false